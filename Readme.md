Hi! Welcome to GDG Mendoza Landing in Polymer

To exe run this commands:

`bower install`
`live-server`

If you are new with bower or live server, first run:

`npm install -g bower`
`npm install -g live-server`